# ── TikTok Voice Generator — Dépendances (2026) ──────────
# Compatible : Ubuntu 22.04+ / Windows 10+ / macOS
# GPU : ROCm 7.x (AMD), CUDA 12.x (NVIDIA), ou CPU fallback

# TTS — Clonage vocal (23 langues, MIT license)
# v0.1.6 inclut Chatterbox-Turbo (350M params, 1-step decoder)
chatterbox-tts>=0.1.6

# STT — Sous-titres avec timestamps mot par mot
# v1.2.1 : support large-v3-turbo, VAD 3x plus rapide, PyAV intégré
faster-whisper>=1.2.1

# Interface web
# Gradio 6.x : nouvelles fonctionnalités, meilleure perf
gradio>=6.0.0

# Audio manipulation
pydub>=0.25.1

# Calcul numérique
numpy>=1.26.0

# HTTP (Pexels + Pixabay API + téléchargements)
requests>=2.31.0

# ── Notes d'installation GPU ─────────────────────────────
# AMD ROCm 7.x (RX 6950 XT, RX 7000/9000 series) :
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm7.0
#
# NVIDIA CUDA 12.x :
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# CPU uniquement :
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
